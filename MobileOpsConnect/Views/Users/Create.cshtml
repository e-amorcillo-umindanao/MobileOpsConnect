@{
    ViewData["Title"] = "Create New User";
    bool isSuperAdmin = ViewBag.IsSuperAdmin ?? false;
}

<!-- Hero Banner -->
<div class="mb-8 mt-2 sm:mb-10">
    <div class="flex flex-wrap items-center gap-2 mb-3">
        <span class="inline-flex items-center gap-1.5 py-1 px-2.5 rounded-full text-[11px] font-semibold bg-emerald-50 text-emerald-600 border border-emerald-100 dark:bg-emerald-900/30 dark:border-emerald-800 dark:text-emerald-400 tracking-wide uppercase">
            <span class="size-1.5 inline-block bg-emerald-500 rounded-full"></span> New Account
        </span>
        @if (isSuperAdmin)
        {
            <span class="inline-flex items-center gap-1.5 py-1 px-2.5 rounded-full text-[11px] font-semibold bg-rose-50 text-rose-600 border border-rose-100 dark:bg-rose-900/30 dark:border-rose-800 dark:text-rose-400 tracking-wide uppercase">
                <i class="bi bi-shield-lock"></i> Super Admin Privilege
            </span>
        }
    </div>
    <h1 class="text-3xl sm:text-[2.5rem] font-bold text-gray-900 dark:text-white mb-2 tracking-tight">Create New User</h1>
    <p class="text-gray-500 text-[15px] dark:text-neutral-400 font-medium">@(isSuperAdmin ? "Register a new System Administrator (Beta) account." : "Add a new team member to the system with the appropriate role and permissions.")</p>
</div>


<!-- Form Card -->
<div class="max-w-3xl mx-auto">
    <div class="flex flex-col bg-white border border-gray-100 shadow-sm rounded-2xl mb-6 dark:bg-neutral-900 dark:border-neutral-800">
        <div class="px-6 py-5 border-b border-gray-100 dark:border-neutral-800">
            <div class="flex items-center gap-x-3">
                <span class="inline-flex justify-center items-center size-[38px] rounded-lg bg-[#f4f2ff] text-[#5e4dcd] dark:bg-indigo-900/30 dark:text-indigo-400"><i class="bi bi-person-vcard text-lg"></i></span>
                <div>
                    <h3 class="text-sm font-bold text-gray-900 dark:text-neutral-200">Account Details</h3>
                    <span class="text-xs text-gray-500 dark:text-neutral-500">Fill in all required fields to create a new user</span>
                </div>
            </div>
        </div>
        <div class="p-6">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="bg-red-50 rounded-lg p-3 text-sm text-red-800 mb-4" role="alert"></div>

                <!-- Email -->
                <div class="mb-5">
                    <label class="block text-sm font-medium mb-2 dark:text-white"><i class="bi bi-envelope me-1 text-indigo-500"></i> Email Address</label>
                    <input name="email" type="email" class="py-3 px-4 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500" required placeholder="user@company.com" />
                </div>

                <!-- Password -->
                <div class="mb-5">
                    <label class="block text-sm font-medium mb-2 dark:text-white"><i class="bi bi-key me-1 text-amber-500"></i> Password</label>
                    <div class="relative">
                        <input name="password" type="password" id="passwordField" class="py-3 px-4 pe-11 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500" required placeholder="Enter a strong password" />
                        <button type="button" id="togglePassword" class="absolute inset-y-0 end-0 flex items-center z-20 px-3 cursor-pointer text-gray-400 rounded-e-md hover:text-blue-600">
                            <i class="bi bi-eye" id="toggleIcon"></i>
                        </button>
                    </div>
                    <div class="mt-2 flex flex-wrap gap-1">
                        <span class="inline-flex items-center gap-x-1 py-0.5 px-2 rounded-md bg-indigo-50 text-indigo-800 text-xs font-medium"><i class="bi bi-check2"></i> Uppercase</span>
                        <span class="inline-flex items-center gap-x-1 py-0.5 px-2 rounded-md bg-indigo-50 text-indigo-800 text-xs font-medium"><i class="bi bi-check2"></i> Number</span>
                        <span class="inline-flex items-center gap-x-1 py-0.5 px-2 rounded-md bg-indigo-50 text-indigo-800 text-xs font-medium"><i class="bi bi-check2"></i> Special Character</span>
                    </div>
                </div>

                <!-- Role -->
                @if (!isSuperAdmin)
                {
                    <div class="mb-5">
                        <label class="block text-sm font-medium mb-2 dark:text-white"><i class="bi bi-shield-check me-1 text-green-500"></i> Assign Role</label>
                        <select name="role" class="py-3 px-4 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500">
                            <option value="DepartmentManager">Department Manager (Charlie)</option>
                            <option value="WarehouseStaff">Warehouse Staff (Delta)</option>
                            <option value="Employee">Employee (Echo)</option>
                        </select>
                        <div class="mt-2">
                            <div class="bg-indigo-50 rounded-lg p-3 flex items-center gap-2">
                                <i class="bi bi-info-circle text-indigo-500"></i>
                                <span class="text-xs text-gray-500">The selected role determines the user's access level and permissions.</span>
                            </div>
                        </div>
                    </div>
                }
                else
                {
                    <input type="hidden" name="role" value="SystemAdmin" />
                }

                <!-- Actions -->
                <div class="flex gap-2">
                    <button type="submit" class="flex-grow py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-semibold rounded-lg border border-[#0d0d0d] bg-[#0d0d0d] text-white hover:bg-[#1a1a1a] transition-colors">
                        <i class="bi bi-person-plus"></i> Create Account
                    </button>
                    <a asp-action="Index" class="py-3 px-4 inline-flex items-center gap-x-2 text-sm font-medium rounded-lg border border-gray-200 bg-white text-gray-800 hover:bg-gray-50 transition-colors dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-300">
                        Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Toggle Password Script -->
<script>
(function () {
    const toggle = document.getElementById('togglePassword');
    const field = document.getElementById('passwordField');
    const icon = document.getElementById('toggleIcon');
    if (!toggle || !field || !icon) return;
    toggle.addEventListener('click', function () {
        const isPassword = field.type === 'password';
        field.type = isPassword ? 'text' : 'password';
        icon.className = isPassword ? 'bi bi-eye-slash' : 'bi bi-eye';
    });
})();
</script>
