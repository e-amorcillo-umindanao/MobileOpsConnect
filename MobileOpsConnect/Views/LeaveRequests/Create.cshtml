@model MobileOpsConnect.Models.LeaveRequest

@{
    ViewData["Title"] = "File Leave Request";
}

<!-- ═══════════════════════════════════════════════ -->
<!-- HERO BANNER                                     -->
<!-- ═══════════════════════════════════════════════ -->
<div class="moc-hero p-6 p-md-5 mb-6 text-white anim-fade-in">
    <div class="position-relative" style="z-index: 2;">
        <div class="flex flex-wrap items-center gap-2 mb-6">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium rounded-pill px-6 py-2 text-sm" style="background:rgba(255,255,255,0.14);">
                <i class="bi bi-circle-fill text-green-500 mr-1" style="font-size:0.4rem;vertical-align:middle;"></i>
                HR Module Active
            </span>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium rounded-pill px-6 py-2 text-sm" style="background:rgba(255,255,255,0.14);">
                <i class="bi bi-pencil-square mr-1"></i> New Application
            </span>
        </div>
        <h1 class="display-5 font-bold mb-2 letter-tight">File Leave Request</h1>
        <p class="lead mb-0 opacity-75">
            Submit a new leave application for manager approval
        </p>
    </div>
    <div class="position-absolute top-50 end-0 translate-middle-y hidden lg:block pe-none"
         style="z-index:1;opacity:0.05;font-size:15rem;line-height:1;margin-right:1.5rem;">
        <i class="bi bi-calendar-plus-fill"></i>
    </div>
</div>

<!-- ═══════════════════════════════════════════════ -->
<!-- QUICK INFO METRICS                              -->
<!-- ═══════════════════════════════════════════════ -->
<div class="moc-section-header mt-2">
    <h5 class="font-bold letter-tight">
        <i class="bi bi-info-circle-fill text-indigo mr-1"></i> Leave Info
    </h5>
    <hr />
</div>

<div class="flex flex-wrap -mx-3 g-3 mb-6">
    <div class="col-6 col-lg-3 anim-fade-in anim-delay-1">
        <div class="moc-metric p-6 p-md-4 h-100">
            <div class="flex items-start justify-between">
                <div>
                    <p class="text-moc-text-muted text-uppercase text-xs font-semibold mb-1">Balance</p>
                    <h3 class="font-bold mb-1 letter-tight">12 <span class="fs-6 font-semibold text-moc-text-muted">days</span></h3>
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-50 text-green text-xs font-semibold">
                        <i class="bi bi-check-circle-fill mr-1"></i> Available
                    </span>
                </div>
                <div class="moc-icon-box bg-green-50 rounded-3">
                    <i class="bi bi-calendar-check text-green fs-5"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-lg-3 anim-fade-in anim-delay-2">
        <div class="moc-metric p-6 p-md-4 h-100">
            <div class="flex items-start justify-between">
                <div>
                    <p class="text-moc-text-muted text-uppercase text-xs font-semibold mb-1">Used</p>
                    <h3 class="font-bold mb-1 letter-tight">3 <span class="fs-6 font-semibold text-moc-text-muted">days</span></h3>
                    <div class="progress mt-1" style="height:4px;width:72px;">
                        <div class="progress-bar rounded-pill" role="progressbar"
                             style="width:20%;background:var(--moc-primary);"
                             aria-valuenow="20" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                    <span class="text-moc-text-muted text-xs">of 15 total</span>
                </div>
                <div class="moc-icon-box bg-indigo-50 rounded-3">
                    <i class="bi bi-calendar-minus text-indigo fs-5"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-lg-3 anim-fade-in anim-delay-3">
        <div class="moc-metric p-6 p-md-4 h-100">
            <div class="flex items-start justify-between">
                <div>
                    <p class="text-moc-text-muted text-uppercase text-xs font-semibold mb-1">Pending</p>
                    <h3 class="font-bold mb-1 letter-tight">1</h3>
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-amber-50 text-amber text-xs font-semibold">
                        <i class="bi bi-hourglass-split mr-1"></i> Waiting
                    </span>
                </div>
                <div class="moc-icon-box bg-amber-50 rounded-3">
                    <i class="bi bi-clock-history text-amber fs-5"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-lg-3 anim-fade-in anim-delay-4">
        <div class="moc-metric p-6 p-md-4 h-100">
            <div class="flex items-start justify-between">
                <div>
                    <p class="text-moc-text-muted text-uppercase text-xs font-semibold mb-1">Approved</p>
                    <h3 class="font-bold mb-1 letter-tight">5</h3>
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-50 text-green text-xs font-semibold">
                        <i class="bi bi-check2-all mr-1"></i> This Year
                    </span>
                </div>
                <div class="moc-icon-box bg-cyan-50 rounded-3">
                    <i class="bi bi-calendar-heart text-cyan fs-5"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- ═══════════════════════════════════════════════ -->
<!-- PUBLIC HOLIDAYS (Nager.Date API)                -->
<!-- ═══════════════════════════════════════════════ -->
@{
    var holidays = (List<MobileOpsConnect.Services.PublicHoliday>?)ViewBag.Holidays ?? new List<MobileOpsConnect.Services.PublicHoliday>();
    var upcomingHolidays = holidays
        .Where(h => h.Date >= DateOnly.FromDateTime(DateTime.Today))
        .OrderBy(h => h.Date)
        .Take(10)
        .ToList();
}

<div class="bg-white shadow-sm rounded-xl border border-moc-border overflow-hidden moc-card border-0 shadow-sm mb-6 anim-fade-in anim-delay-2">
    <div class="px-6 py-4 border-b border-moc-border bg-gray-50 bg-transparent border-0 p-6 pb-0">
        <div class="flex items-center justify-between">
            <div class="flex items-center gap-2">
                <div class="moc-icon-box bg-secondary-50 rounded-3">
                    <i class="bi bi-calendar-event text-secondary-color fs-5"></i>
                </div>
                <div>
                    <h6 class="font-bold mb-0 letter-tight">
                        <i class="bi bi-broadcast mr-1"></i> Philippine Public Holidays @DateTime.Now.Year
                    </h6>
                    <span class="text-moc-text-muted text-xs">
                        Upcoming holidays &bull; Data from <strong>Nager.Date API</strong>
                    </span>
                </div>
            </div>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium rounded-pill bg-indigo-50 text-indigo px-6 py-2 text-xs font-semibold">
                <i class="bi bi-lightning-charge-fill mr-1"></i> Live API
            </span>
        </div>
    </div>
    <div class="p-6 p-6">
        @if (upcomingHolidays.Any())
        {
            <div class="overflow-x-auto w-full">
                <table class="w-full text-left border-collapse align-middle mb-0" style="border-collapse:separate;border-spacing:0 0.25rem;">
                    <thead class="bg-light">
                        <tr>
                            <th class="text-uppercase text-xs font-semibold text-moc-text-muted border-0 ps-3 py-2" style="letter-spacing:0.05em;">Date</th>
                            <th class="text-uppercase text-xs font-semibold text-moc-text-muted border-0 py-2" style="letter-spacing:0.05em;">Holiday</th>
                            <th class="text-uppercase text-xs font-semibold text-moc-text-muted border-0 py-2" style="letter-spacing:0.05em;">English Name</th>
                            <th class="text-uppercase text-xs font-semibold text-moc-text-muted border-0 pe-3 py-2 text-center" style="letter-spacing:0.05em;">Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var h in upcomingHolidays)
                        {
                            var isPast = h.Date < DateOnly.FromDateTime(DateTime.Today);
                            var daysUntil = h.Date.DayNumber - DateOnly.FromDateTime(DateTime.Today).DayNumber;
                            <tr class="holiday-row" data-date="@h.Date.ToString("yyyy-MM-dd")">
                                <td class="border-0 ps-3 py-2">
                                    <span class="font-semibold text-sm">@h.Date.ToString("MMM dd, yyyy")</span>
                                    <br />
                                    <span class="text-moc-text-muted text-xs">@h.Date.DayOfWeek</span>
                                </td>
                                <td class="border-0 py-2">
                                    <span class="font-semibold text-sm">@h.LocalName</span>
                                </td>
                                <td class="border-0 py-2">
                                    <span class="text-moc-text-muted text-sm">@h.Name</span>
                                </td>
                                <td class="border-0 pe-3 py-2 text-center">
                                    @if (daysUntil == 0)
                                    {
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium rounded-pill px-2 py-1 text-xs" style="background:rgba(34,197,94,0.1);color:#16a34a;">
                                            <i class="bi bi-star-fill mr-1"></i>Today
                                        </span>
                                    }
                                    else if (daysUntil <= 7)
                                    {
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium rounded-pill px-2 py-1 text-xs" style="background:rgba(245,158,11,0.1);color:#d97706;">
                                            In @daysUntil day@(daysUntil != 1 ? "s" : "")
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium rounded-pill px-2 py-1 text-xs" style="background:rgba(99,102,241,0.08);color:#6366f1;">
                                            In @daysUntil days
                                        </span>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <div class="mt-2 text-center">
                <span class="text-moc-text-muted text-xs">
                    <i class="bi bi-info-circle mr-1"></i>
                    Holidays that overlap with your leave dates will be highlighted &bull; Source: <strong>date.nager.at</strong>
                </span>
            </div>
        }
        else
        {
            <div class="text-center py-6">
                <div class="moc-icon-box-lg rounded-3 mx-auto mb-6" style="width:56px;height:56px;background:rgba(239,68,68,0.06);">
                    <i class="bi bi-wifi-off fs-3" style="color:#ef4444;"></i>
                </div>
                <p class="font-semibold text-sm mb-1 letter-tight">Holiday data unavailable</p>
                <span class="text-moc-text-muted text-xs">Unable to fetch holidays from the API.</span>
            </div>
        }
    </div>
</div>

<!-- ═══════════════════════════════════════════════ -->
<!-- LEAVE APPLICATION FORM                          -->
<!-- ═══════════════════════════════════════════════ -->
<div class="moc-section-header mt-8">
    <h5 class="font-bold letter-tight">
        <i class="bi bi-pencil-square text-green mr-1"></i> Application Form
    </h5>
    <hr />
</div>

<div class="flex flex-wrap -mx-3 justify-center">
    <div class="w-full lg:w-2/3 px-6 col-xl-7">

        <div class="bg-white shadow-sm rounded-xl border border-moc-border overflow-hidden moc-card border-0 shadow-sm mb-6 anim-fade-in anim-delay-1">
            <div class="p-6 p-6 p-md-5">

                <form asp-action="Create">

                    <!-- ── Leave Type Section ── -->
                    <div class="flex items-center mb-6">
                        <div class="moc-icon-box bg-cyan-50 rounded-3 mr-4" style="width:40px;height:40px;min-width:40px;">
                            <i class="bi bi-bookmark-star-fill text-cyan"></i>
                        </div>
                        <div>
                            <h6 class="font-bold mb-0 letter-tight">Leave Type</h6>
                            <span class="text-moc-text-muted text-xs">Select the category for your leave</span>
                        </div>
                    </div>

                    <div class="mb-6">
                        <label asp-for="LeaveType" class="block text-sm font-medium leading-6 text-gray-900 font-semibold text-sm mb-2">
                            <i class="bi bi-tag-fill mr-1 text-cyan"></i> Type of Leave
                        </label>
                        <select asp-for="LeaveType" class="block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 form-select-lg rounded-3"
                                style="padding:0.875rem 1rem;font-size:1rem;">
                            <option>Sick Leave</option>
                            <option>Vacation Leave</option>
                            <option>Emergency Leave</option>
                        </select>
                    </div>

                    <hr class="my-6 opacity-10" />

                    <!-- ── Date Range Section ── -->
                    <div class="flex items-center mb-6">
                        <div class="moc-icon-box bg-indigo-50 rounded-3 mr-4" style="width:40px;height:40px;min-width:40px;">
                            <i class="bi bi-calendar-range-fill text-indigo"></i>
                        </div>
                        <div>
                            <h6 class="font-bold mb-0 letter-tight">Date Range</h6>
                            <span class="text-moc-text-muted text-xs">Choose the start and end dates</span>
                        </div>
                    </div>

                    <div class="flex flex-wrap -mx-3 g-3 mb-6">
                        <div class="col-sm-6">
                            <label asp-for="StartDate" class="block text-sm font-medium leading-6 text-gray-900 font-semibold text-sm mb-2">
                                <i class="bi bi-calendar-event mr-1 text-green"></i> Start Date
                            </label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text rounded-start-3 border-end-0"
                                      style="background:rgba(34,197,94,0.06);">
                                    <i class="bi bi-calendar-date text-green"></i>
                                </span>
                                <input asp-for="StartDate" class="block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 rounded-end-3 border-start-0" type="date"
                                       style="padding:0.875rem 1rem;font-size:1rem;" />
                            </div>
                        </div>

                        <div class="col-sm-6">
                            <label asp-for="EndDate" class="block text-sm font-medium leading-6 text-gray-900 font-semibold text-sm mb-2">
                                <i class="bi bi-calendar-event mr-1 text-red"></i> End Date
                            </label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text rounded-start-3 border-end-0"
                                      style="background:rgba(239,68,68,0.06);">
                                    <i class="bi bi-calendar-date text-red"></i>
                                </span>
                                <input asp-for="EndDate" class="block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 rounded-end-3 border-start-0" type="date"
                                       style="padding:0.875rem 1rem;font-size:1rem;" />
                            </div>
                        </div>
                    </div>

                    <hr class="my-6 opacity-10" />

                    <!-- ── Reason Section ── -->
                    <div class="flex items-center mb-6">
                        <div class="moc-icon-box bg-amber-50 rounded-3 mr-4" style="width:40px;height:40px;min-width:40px;">
                            <i class="bi bi-chat-left-text-fill text-amber"></i>
                        </div>
                        <div>
                            <h6 class="font-bold mb-0 letter-tight">Reason</h6>
                            <span class="text-moc-text-muted text-xs">Provide a brief explanation</span>
                        </div>
                    </div>

                    <div class="mb-6">
                        <label asp-for="Reason" class="block text-sm font-medium leading-6 text-gray-900 font-semibold text-sm mb-2">
                            <i class="bi bi-chat-dots mr-1 text-amber"></i> Reason for Leave
                        </label>
                        <textarea asp-for="Reason" class="block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 rounded-3" rows="4"
                                  placeholder="Why do you need this leave?"
                                  style="padding:0.875rem 1rem;font-size:1rem;resize:vertical;min-height:120px;"></textarea>
                        <small class="text-moc-text-muted text-xs mt-1 flex items-center">
                            <i class="bi bi-info-circle mr-1 text-indigo"></i>
                            Be clear and concise — this helps your manager process faster.
                        </small>
                    </div>

                    <hr class="my-6 opacity-10" />

                    <!-- ── Policy Notice ── -->
                    <div class="rounded-3 p-6 mb-6 flex items-start gap-6"
                         style="background:rgba(99,102,241,0.04);border:1px solid rgba(99,102,241,0.1);">
                        <div class="moc-icon-box bg-indigo-50 rounded-3" style="width:40px;height:40px;min-width:40px;">
                            <i class="bi bi-shield-check text-indigo"></i>
                        </div>
                        <div>
                            <p class="font-semibold text-sm mb-1 letter-tight">Leave Policy Reminder</p>
                            <p class="text-moc-text-muted text-xs mb-0">
                                Leave requests should be filed at least 3 working days in advance (except emergencies).
                                Your manager will be notified and must approve before the leave becomes effective.
                            </p>
                        </div>
                    </div>

                    <!-- ── Action Buttons ── -->
                    <div class="d-grid gap-2">
                        <input type="submit" value="Submit Request" class="inline-flex justify-center rounded-md px-3 py-2 text-sm font-semibold shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 btn-moc-primary px-4 py-2 text-base py-6" />
                        <a asp-controller="Home" asp-action="Index" class="inline-flex justify-center rounded-md px-3 py-2 text-sm font-semibold shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 btn-moc-outline px-4 py-2 text-base py-6">
                            <i class="bi bi-arrow-left mr-1"></i> Cancel &amp; Go Back
                        </a>
                    </div>

                </form>

            </div>

            <!-- Card Footer -->
            <div class="px-6 py-4 border-t border-moc-border bg-gray-50 bg-transparent border-0 px-6 py-6 text-center"
                 style="border-top:1px solid var(--moc-border);border-radius:0 0 0.875rem 0.875rem;">
                <span class="text-moc-text-muted text-xs">
                    <i class="bi bi-lock-fill mr-1"></i>
                    Your request is securely submitted and visible only to authorized approvers.
                </span>
            </div>
        </div>

    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const startDateInput = document.getElementById('StartDate');
            const endDateInput = document.getElementById('EndDate');

            // Default start date to today
            if (startDateInput && !startDateInput.value) {
                const today = new Date().toISOString().split('T')[0];
                startDateInput.value = today;
                endDateInput.min = today;
            }

            // On start date change: set min on end date, auto-focus end date
            if (startDateInput && endDateInput) {
                startDateInput.addEventListener('change', function () {
                    if (this.value) {
                        endDateInput.min = this.value;

                        // Clear end date if it's now before start date
                        if (endDateInput.value && endDateInput.value < this.value) {
                            endDateInput.value = '';
                        }

                        // Auto-focus end date field
                        endDateInput.focus();
                        // Try to open the date picker (works in most browsers)
                        if (typeof endDateInput.showPicker === 'function') {
                            try { endDateInput.showPicker(); } catch (e) { /* ignore */ }
                        }
                    }
                });
            }
        });
    </script>
}
