@model PaginatedList<MobileOpsConnect.Models.PurchaseOrder>
@{
    ViewData["Title"] = "Purchase Orders";
    string currentStatus = ViewBag.CurrentStatus ?? "active";
}

<!-- Breadcrumb Navigation -->
<div class="mb-6">
    <a asp-controller="Home" asp-action="Index" class="text-sm font-medium text-gray-400 hover:text-gray-600 dark:text-neutral-500 dark:hover:text-neutral-300 transition-colors">
        ← Back to Dashboard
    </a>
</div>

<!-- Hero Banner -->
<div class="mb-8 mt-2 sm:mb-10">
    <h1 class="text-3xl sm:text-[2.5rem] font-bold text-gray-900 dark:text-white mb-2 tracking-tight">Purchase Orders</h1>
    <p class="text-gray-500 text-[15px] dark:text-neutral-400 font-medium">Restock requests, approval tracking & order history</p>
</div>

<!-- Metrics Grid -->
<div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 mb-8">
    <div class="flex flex-col bg-white border shadow-sm rounded-xl dark:bg-neutral-900 dark:border-neutral-800">
        <div class="p-4 md:p-5 flex gap-x-4">
            <div class="shrink-0 flex justify-center items-center size-[46px] bg-indigo-100 text-indigo-800 rounded-lg dark:bg-indigo-800/30 dark:text-indigo-500">
                <i class="bi bi-clipboard2-data-fill text-xl"></i>
            </div>
            <div class="grow">
                <div class="flex justify-between items-start">
                    <p class="text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-500">Total POs</p>
                    <h3 class="text-xl sm:text-2xl font-bold text-gray-800 dark:text-neutral-200">@ViewBag.TotalCount</h3>
                </div>
            </div>
        </div>
    </div>

    <div class="flex flex-col bg-white border shadow-sm rounded-xl dark:bg-neutral-900 dark:border-neutral-800">
        <div class="p-4 md:p-5 flex gap-x-4">
            <div class="shrink-0 flex justify-center items-center size-[46px] bg-amber-100 text-amber-800 rounded-lg dark:bg-amber-800/30 dark:text-amber-500">
                <i class="bi bi-clock-history text-xl"></i>
            </div>
            <div class="grow">
                <div class="flex justify-between items-start">
                    <p class="text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-500">Pending</p>
                    <h3 class="text-xl sm:text-2xl font-bold text-gray-800 dark:text-neutral-200">@ViewBag.PendingCount</h3>
                </div>
            </div>
        </div>
    </div>

    <div class="flex flex-col bg-white border shadow-sm rounded-xl dark:bg-neutral-900 dark:border-neutral-800">
        <div class="p-4 md:p-5 flex gap-x-4">
            <div class="shrink-0 flex justify-center items-center size-[46px] bg-green-100 text-green-800 rounded-lg dark:bg-green-800/30 dark:text-green-500">
                <i class="bi bi-check2-all text-xl"></i>
            </div>
            <div class="grow">
                <div class="flex justify-between items-start">
                    <p class="text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-500">Approved</p>
                    <h3 class="text-xl sm:text-2xl font-bold text-gray-800 dark:text-neutral-200">@ViewBag.ApprovedCount</h3>
                </div>
            </div>
        </div>
    </div>

    <div class="flex flex-col bg-white border shadow-sm rounded-xl dark:bg-neutral-900 dark:border-neutral-800">
        <div class="p-4 md:p-5 flex gap-x-4">
            <div class="shrink-0 flex justify-center items-center size-[46px] rounded-lg @(ViewBag.RejectedCount > 0 ? "bg-red-100 text-red-800 dark:bg-red-800/30 dark:text-red-500" : "bg-green-100 text-green-800 dark:bg-green-800/30 dark:text-green-500")">
                <i class="bi @(ViewBag.RejectedCount > 0 ? "bi-x-octagon-fill" : "bi-check2-all") text-xl"></i>
            </div>
            <div class="grow">
                <div class="flex justify-between items-start">
                    <p class="text-xs uppercase tracking-wide text-gray-500 dark:text-neutral-500">Rejected</p>
                    <h3 class="text-xl sm:text-2xl font-bold text-gray-800 dark:text-neutral-200">@ViewBag.RejectedCount</h3>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Action Bar & Tab Interface -->
<div class="flex flex-wrap justify-between items-end gap-4 border-b border-gray-200 dark:border-neutral-700 mb-6">
    <!-- Tab Header Underline -->
    <nav class="flex space-x-6" aria-label="Tabs">
        <a asp-action="Index" asp-route-status="active" 
           class="tab-link py-3 px-1 inline-flex items-center gap-2 border-b-2 text-sm whitespace-nowrap font-bold focus:outline-none focus:ring-0 transition-colors @(currentStatus == "active" ? "border-gray-800 text-gray-900 dark:border-white dark:text-white active-tab" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-neutral-400 inactive-tab")">
            <i class="bi bi-hourglass-split"></i> Active
            <span class="inline-flex items-center py-0.5 px-2 rounded-full text-[10px] font-bold @(currentStatus == "active" ? "bg-gray-100 text-gray-800 dark:bg-neutral-700 dark:text-white" : "bg-gray-50 text-gray-500 dark:bg-neutral-800 dark:text-neutral-400")">@ViewBag.PendingCount</span>
        </a>
        <a asp-action="Index" asp-route-status="archived" 
           class="tab-link py-3 px-1 inline-flex items-center gap-2 border-b-2 text-sm whitespace-nowrap font-bold focus:outline-none focus:ring-0 transition-colors @(currentStatus == "archived" ? "border-gray-800 text-gray-900 dark:border-white dark:text-white active-tab" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-neutral-400 inactive-tab")">
            <i class="bi bi-archive"></i> Archive
            <span class="inline-flex items-center py-0.5 px-2 rounded-full text-[10px] font-bold @(currentStatus == "archived" ? "bg-gray-100 text-gray-800 dark:bg-neutral-700 dark:text-white" : "bg-gray-50 text-gray-500 dark:bg-neutral-800 dark:text-neutral-400")">@(ViewBag.ApprovedCount + ViewBag.RejectedCount)</span>
        </a>
    </nav>

    @if (User.IsInRole("DepartmentManager") || User.IsInRole("WarehouseStaff") || User.IsInRole("SuperAdmin"))
    {
        <div class="pb-3">
            <a asp-action="Create" class="py-2 px-4 inline-flex items-center gap-x-2 text-xs font-bold rounded-lg border border-[#0d0d0d] bg-[#0d0d0d] text-white hover:bg-[#1a1a1a] transition-colors">
                <i class="bi bi-plus-circle"></i> Submit New PO
            </a>
        </div>
    }
</div>

<!-- Order Table Container (Targeted by AJAX) -->
<div id="table-container">
    <partial name="_OrdersTable" model="Model" />
</div>

<!-- Tab Toggle & Pagination Script (Native AJAX) -->
<script>
    (function () {
        function loadContent(url, updateHistory = true) {
            const container = document.getElementById('table-container');
            if (!container) return;

            container.style.opacity = '0.5';

            fetch(url, {
                headers: { 'X-Requested-With': 'XMLHttpRequest' }
            })
            .then(response => {
                if (!response.ok) throw new Error();
                return response.text();
            })
            .then(html => {
                container.innerHTML = html;
                container.style.opacity = '1';
                if (updateHistory) history.pushState(null, '', url);
                updateActiveTab(url);
            })
            .catch(() => {
                // Graceful degradation on failure: full page reload
                window.location.href = url;
            });
        }

        function updateActiveTab(url) {
            const params = new URLSearchParams(new URL(url, location.origin).search);
            const status = params.get('status') || 'active'; // Match casing of default status
            
            document.querySelectorAll('.tab-link').forEach(tab => {
                const tabStatus = new URLSearchParams(
                    new URL(tab.href, location.origin).search
                ).get('status') || 'active';
                
                if (tabStatus === status) {
                    tab.classList.remove('border-transparent', 'text-gray-500', 'hover:text-gray-700', 'dark:text-neutral-400', 'inactive-tab');
                    tab.classList.add('border-gray-800', 'text-gray-900', 'dark:border-white', 'dark:text-white', 'active-tab');
                    
                    const badge = tab.querySelector('span');
                    if(badge) {
                        badge.classList.remove('bg-gray-50', 'text-gray-500', 'dark:bg-neutral-800', 'dark:text-neutral-400');
                        badge.classList.add('bg-gray-100', 'text-gray-800', 'dark:bg-neutral-700', 'dark:text-white');
                    }
                } else {
                    tab.classList.remove('border-gray-800', 'text-gray-900', 'dark:border-white', 'dark:text-white', 'active-tab');
                    tab.classList.add('border-transparent', 'text-gray-500', 'hover:text-gray-700', 'dark:text-neutral-400', 'inactive-tab');
                    
                    const badge = tab.querySelector('span');
                    if(badge) {
                        badge.classList.remove('bg-gray-100', 'text-gray-800', 'dark:bg-neutral-700', 'dark:text-white');
                        badge.classList.add('bg-gray-50', 'text-gray-500', 'dark:bg-neutral-800', 'dark:text-neutral-400');
                    }
                }
            });
        }

        // Event delegation - handles tabs AND dynamic pagination links
        document.addEventListener('click', (e) => {
            const link = e.target.closest('.tab-link, #table-container a[href]');
            if (link) {
                e.preventDefault();
                loadContent(link.getAttribute('href'));
            }
        });

        // Browser Back/Forward support
        window.addEventListener('popstate', () => {
            loadContent(window.location.href, false);
        });
    })();
</script>
